# 跨域问题解决方案

## 📋 问题分析

### 当前情况

1. **Vue 管理端**：
   - 直接访问 `http://localhost:3000` → ❌ 跨域错误
   - 通过 Nginx `http://localhost` → ✅ 正常（同源请求）

2. **UniApp 用户端**：
   - H5 开发服务器可能使用不同端口 → ❌ 跨域错误
   - 需要配置正确的 CORS 或使用代理

3. **后端 CORS 配置**：
   - 只允许了 `localhost:3000` 和 `localhost`
   - 缺少 UniApp 开发服务器的端口

---

## ✅ 解决方案

### 方案一：扩展后端 CORS 配置（推荐）⭐

**优点**：
- ✅ 灵活性高，支持多种开发场景
- ✅ 不需要修改前端配置
- ✅ 开发调试方便

**已更新**：`SecurityConfig.java` 已添加以下允许的源：
- `http://localhost:5173` - UniApp H5 开发服务器（Vite 默认端口）
- `http://127.0.0.1:5173`
- `http://localhost:8080` - UniApp H5 开发服务器（如果配置为此端口）
- `http://127.0.0.1:8080`
- `http://localhost:5174` - 备用端口

**使用方式**：
1. 重启后端服务
2. Vue 管理端：直接访问 `http://localhost:3000`
3. UniApp H5：使用 HBuilderX 运行到浏览器

---

### 方案二：使用 Nginx 统一代理（当前可用）

**优点**：
- ✅ 统一入口，配置集中
- ✅ 不需要 CORS（同源请求）
- ✅ 生产环境推荐方案

**配置**：已配置 `nginx-dev.conf`

**使用方式**：
1. 启动 Nginx（使用 `nginx-dev.conf`）
2. Vue 管理端：访问 `http://localhost`
3. UniApp H5：需要配置代理（见下方）

**Nginx 配置**：
```nginx
server {
    listen 80;
    server_name localhost;

    # 前端开发服务器代理
    location / {
        proxy_pass http://localhost:3000;  # Vue 管理端
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:8080;
    }
}
```

---

### 方案三：UniApp 使用 Nginx 代理（推荐用于 UniApp）

**配置 UniApp manifest.json**：

```json
{
  "h5": {
    "devServer": {
      "port": 5173,
      "proxy": {
        "/api": {
          "target": "http://localhost:8080",
          "changeOrigin": true,
          "secure": false
        }
      }
    }
  }
}
```

**或者使用 Nginx 代理 UniApp**：

在 `nginx-dev.conf` 中添加：

```nginx
# UniApp H5 开发服务器代理
location /uniapp {
    proxy_pass http://localhost:5173;
    
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

---

### 方案四：Vue 管理端启用 Vite 代理（临时方案）

**修改 `Vue/vite.config.js`**：

```javascript
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        secure: false
      }
    }
  }
})
```

**注意**：如果使用 Nginx，需要注释掉 proxy 配置（当前已注释）

---

## 🎯 推荐配置方案

### 开发环境

| 前端 | 访问地址 | 后端 CORS | 说明 |
|------|---------|----------|------|
| Vue 管理端 | `http://localhost:3000` | ✅ 已配置 | 直接访问，需要 CORS |
| Vue 管理端 | `http://localhost` | ❌ 不需要 | 通过 Nginx，同源请求 |
| UniApp H5 | `http://localhost:5173` | ✅ 已配置 | 直接访问，需要 CORS |
| UniApp H5 | `http://localhost/uniapp` | ❌ 不需要 | 通过 Nginx，同源请求 |

### 推荐组合

**组合1：全部使用 Nginx（生产环境推荐）**
- Vue 管理端：`http://localhost` → Nginx → `localhost:3000`
- UniApp H5：`http://localhost/uniapp` → Nginx → `localhost:5173`
- API：`http://localhost/api` → Nginx → `localhost:8080`
- **优点**：统一入口，不需要 CORS
- **缺点**：需要配置 Nginx

**组合2：直接访问 + CORS（开发环境推荐）**
- Vue 管理端：`http://localhost:3000` → 直接访问后端
- UniApp H5：`http://localhost:5173` → 直接访问后端
- **优点**：配置简单，开发方便
- **缺点**：需要后端 CORS 支持（已配置）

---

## 🔧 具体操作步骤

### 步骤1：更新后端 CORS 配置（已完成）

已更新 `SecurityConfig.java`，添加了 UniApp 常用端口。

**需要重启后端服务**：
```bash
cd Java
mvn spring-boot:run
```

### 步骤2：配置 UniApp H5 开发服务器

**选项A：使用 Vite 代理（推荐）**

修改 `UniApp/智评优选/manifest.json`：

```json
{
  "h5": {
    "devServer": {
      "port": 5173,
      "proxy": {
        "/api": {
          "target": "http://localhost:8080",
          "changeOrigin": true,
          "secure": false
        }
      }
    }
  }
}
```

**选项B：修改 API 基础 URL**

修改 `UniApp/智评优选/utils/constants.js`：

```javascript
// 开发环境：使用 Nginx 代理
export const BASE_URL = 'http://localhost/api'

// 或直接访问后端（需要 CORS）
export const BASE_URL = 'http://localhost:8080/api'
```

### 步骤3：测试

1. **Vue 管理端**：
   - 访问 `http://localhost:3000` → 应该可以正常登录
   - 如果不行，检查后端是否重启

2. **UniApp H5**：
   - 在 HBuilderX 中运行到浏览器
   - 检查控制台是否有跨域错误
   - 如果使用代理，确保代理配置正确

---

## ⚠️ 常见问题

### Q1: Vue 管理端 `localhost:3000` 仍然无法访问？

**检查**：
1. 后端服务是否已重启（CORS 配置需要重启生效）
2. 浏览器控制台的具体错误信息
3. 网络请求的响应头是否包含 `Access-Control-Allow-Origin`

**解决**：
- 清除浏览器缓存
- 检查后端日志，确认 CORS 配置生效
- 使用浏览器开发者工具查看网络请求详情

### Q2: UniApp H5 仍然跨域？

**检查**：
1. UniApp H5 开发服务器的实际端口（查看 HBuilderX 控制台）
2. 如果端口不是 5173，需要添加到后端 CORS 配置

**解决**：
- 查看 HBuilderX 运行日志，确认端口号
- 将端口添加到 `SecurityConfig.java` 的 `allowedOrigins` 列表
- 或使用代理配置（推荐）

### Q3: 生产环境怎么办？

**建议**：
1. 使用 Nginx 统一代理（不需要 CORS）
2. 后端 CORS 配置改为生产域名：
   ```java
   configuration.setAllowedOrigins(Arrays.asList(
       "https://your-frontend-domain.com",
       "https://your-uniapp-domain.com"
   ));
   ```

---

## 📝 总结

### 当前最佳方案

1. **开发环境**：
   - ✅ 后端 CORS 已扩展（支持多个端口）
   - ✅ Vue 管理端：直接访问 `localhost:3000`
   - ✅ UniApp H5：使用代理或直接访问（需要 CORS）

2. **生产环境**：
   - ✅ 使用 Nginx 统一代理
   - ✅ 后端 CORS 配置生产域名
   - ✅ 所有请求通过 Nginx，同源请求

### 下一步操作

1. **重启后端服务**（使 CORS 配置生效）
2. **测试 Vue 管理端**：访问 `http://localhost:3000`，尝试登录
3. **测试 UniApp H5**：在 HBuilderX 中运行，检查是否正常
4. **如果还有问题**：查看浏览器控制台错误，根据错误信息调整

---

**更新时间**: 2026-1-10  
**状态**: 后端 CORS 配置已更新，需要重启后端服务

