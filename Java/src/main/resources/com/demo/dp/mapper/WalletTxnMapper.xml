<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dp.mapper.WalletTxnMapper">

    <insert id="insert" parameterType="com.demo.dp.domain.entity.WalletTxn" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wallet_txn (
            user_id, type, amount, balance_before, balance_after, biz_no, remark, created_at
        ) VALUES (
            #{userId}, #{type}, #{amount}, #{balanceBefore}, #{balanceAfter}, #{bizNo}, #{remark}, NOW()
        )
    </insert>

</mapper>
